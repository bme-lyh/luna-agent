interface:
  display_name: "Luna Agent"
  short_description: "Run native GPT-5.6 Luna subagents"
  default_prompt: "Use $luna-agent with agents=auto and effort=max to delegate this objective to native Luna workers."

policy:
  allow_implicit_invocation: true
